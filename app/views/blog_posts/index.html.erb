<div class="max-w-3xl mx-auto py-8 px-4 sm:px-6">
    <h1 class="text-3xl font-bold text-slate-800 mb-8">My Blog</h1>
    <div class="space-y-8">
        <% @blog_posts.each do |blog_post|  %>
        <h2 class="text-2xl font-semibold text-indigo-600 mb-2 hover:text-indigo-800 transition-colors"><%= link_to blog_post.title , blog_post %></h2>
        <p class="prose text-slate-600"><%= blog_post.body %></p>
        <div class="mt-4">
            <%= link_to "Read more →", blog_post, class: "text-indigo-600 hover:text-indigo-800 hover:underline text-sm font-medium" %>
        </div>
        <% end %>

    </div>
</div>